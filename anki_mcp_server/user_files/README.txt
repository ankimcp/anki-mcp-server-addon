Any files placed in this folder are preserved when the add-on is upgraded.

This directory holds the add-on's tunnel OAuth credentials
(credentials.json). It is owned by the add-on and is NOT shared with the
AnkiMCP CLI.
